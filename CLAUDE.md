# NEWSMAKER - Описание архитектуры для Claude

## Обзор проекта
NEWSMAKER - автоматический сервис для получения и публикации российских юридических новостей в Telegram. Система работает ежедневно, собирая 7 законодательных новостей утром через Perplexity Deep Research, генерируя 4-панельные комиксы с помощью OpenAI, и публикуя их равномерно в течение дня в Telegram канале.

## Новая архитектура (2025) 🆕

### Основной принцип
**Разделение сбора и публикации:**
- **8:30 утра** - сбор 7 новостей + генерация всех изображений
- **09:05 - 21:07** - публикация по расписанию из готовых файлов

### Расписание публикаций (7 времён)
```
09:05 - Приоритет 1 (критическая важность)
11:03 - Приоритет 2 (очень важная)
13:07 - Приоритет 3 (важная)
15:09 - Приоритет 4 (средняя)
17:05 - Приоритет 5 (умеренная)
19:02 - Приоритет 6 (дополнительная)  🆕
21:07 - Приоритет 7 (низкая)  🆕
```

### Преимущества новой архитектуры
✅ **Скорость:** Публикация за 6 секунд вместо 60-120  
✅ **Контроль:** Все тексты и изображения доступны до публикации  
✅ **Экономия:** Quality "medium" вместо "high"  
✅ **Равномерность:** Новости растянуты на весь день  

## Архитектура системы

### Основные компоненты

1. **main.py** - точка входа приложения
   - Обрабатывает аргументы командной строки
   - Управляет режимами работы (тест, сбор, публикация, планировщик)
   - Инициализация логирования и проверка .env файла

2. **news_collector.py** 🆕 - ежедневный сбор новостей
   - Использует Perplexity Deep Research (sonar-deep-research)
   - Собирает РОВНО 7 новостей с приоритизацией
   - **Генерирует ВСЕ изображения сразу** и сохраняет в файлы
   - Максимальное использование токенов (8192)

3. **news_publisher.py** 🆕 - публикация из файлов
   - Читает готовые новости и изображения
   - **Мгновенная публикация** без генерации
   - Антидубликатор и проверка расписания
   - Поддержка принудительной и тестовой публикации

4. **news_scheduler.py** 🆕 - новый планировщик
   - Управляет сбором в 8:30 и публикациями 7 раз в день
   - Интегрирует все компоненты новой архитектуры
   - Автоматическое восстановление после ошибок

5. **prompts.py** - централизованные промпты 🔄
   - **Новый промпт для 7 новостей** вместо 5-6
   - **4-панельный комикс БЕЗ текста** в реалистичном стиле
   - Quality: "medium" для экономии
   - 7 уровней приоритета

6. **openai_client.py** - генерация 4-панельных комиксов
   - Использует модель `gpt-image-1`
   - Создает реалистичные 4-панельные комиксы без текста
   - Размер: 1536x1024, качество: medium
   - Визуальная история: обнаружение → реакция → понимание → адаптация

7. **telegram_client.py** - публикация в Telegram
   - Поддержка отправки как текста, так и изображений
   - HTML-форматирование с кликабельными ссылками
   - Разбивка длинных сообщений на части
   - **Антидубликатор** - проверка на повторные публикации

8. **config.py** - конфигурация 🔄
   - API ключи из .env (безопасность)
   - **7 времён публикации** в PUBLICATION_SCHEDULE
   - Время сбора: 8:30
   - Настройки Deep Research

9. **scheduler.py** - legacy планировщик (совместимость)
   - Обёртка для новой архитектуры
   - Поддержка старых команд
   - Автоматическое переключение режимов

## Workflow (рабочий процесс)

### Ежедневный цикл:

#### 1. **Утренний сбор (8:30)** - news_collector.py
- Запрос к Perplexity Deep Research (8192 токена)
- Парсинг 7 новостей с приоритизацией 1-7
- **Генерация 7 изображений** подряд (~20 минут)
- Сохранение в `data/daily_news_YYYY-MM-DD.json`
- Изображения в `data/images/YYYY-MM-DD/news_*.png`

#### 2. **Публикации в течение дня** - news_publisher.py
- 09:05 → Загрузка готового изображения приоритет 1 → Публикация (6 сек)
- 11:03 → Загрузка готового изображения приоритет 2 → Публикация (6 сек)
- ... (5 раз до 21:07)

### Режимы работы:
```bash
# Основные команды новой архитектуры
python main.py --collect          # Ручной сбор новостей
python main.py --publish-next     # Публикация следующей новости
python main.py --force-publish N  # Публикация приоритет N (1-7)
python main.py --test-publish-all # Тест: все новости подряд с интервалом

# Системные команды
python main.py --status           # Статус системы
python main.py --start           # Запуск планировщика
python main.py --test            # Тестирование компонентов

# Legacy команды (совместимость)
python main.py --run             # Старый режим разового запуска
python main.py --info            # Информация о системе
```

## Ключевые особенности

### Новые возможности 🆕
- **Предварительный просмотр:** Все изображения и тексты доступны сразу после утреннего сбора
- **7 новостей в день:** Равномерное распределение с 09:05 до 21:07
- **4-панельные комиксы:** Визуальная история без текста
- **Мгновенная публикация:** 6 секунд вместо 60-120
- **Экономия:** Quality "medium" = дешевле

### Надежность
- Retry-логика для всех API вызовов
- Graceful fallback если изображения не генерируются
- Детальное логирование для отладки
- **Антидубликатор** - файловое хранение истории с проверкой схожести

### Форматирование контента
- Автоматическое HTML-форматирование для Telegram
- Очистка от markdown артефактов Deep Research
- Кликабельные ссылки на источники
- Разбивка длинных сообщений на части

### Конфигурация
- API ключи в .env файле (безопасность)
- 7 времён публикации в config.py
- Интервал сбора: ежедневно в 8:30
- Таймауты и retry настройки

## Структура данных

### Формат файла новостей:
```json
{
  "date": "2025-07-19",
  "collected_at": "2025-07-20T08:30:15",
  "total_news": 7,
  "news": [
    {
      "priority": 1,
      "title": "Название новости",
      "content": "📜 Заголовок\n\n💬 КОММЕНТАРИЙ...",
      "sources": ["https://url1", "https://url2"],
      "scheduled_time": "09:05",
      "id": "news_20250720_1",
      "published": false,
      "publication_attempts": 0,
      "image_path": "data/images/2025-07-19/news_20250720_1.png",
      "image_generated": true,
      "image_size": 2374882
    }
  ]
}
```

### Формат 4-панельного комикса:
- **Панель 1:** Персонаж обнаруживает изменение (читает телефон/газету)
- **Панель 2:** Реакция - обрабатывает информацию (удивление/растерянность)
- **Панель 3:** Понимание - осознает последствия (момент "ага!")
- **Панель 4:** Адаптация - принимает новую реальность (действие/принятие)

## Безопасность
- API ключи только в .env файле
- Валидация входных данных перед отправкой
- Антидубликатор против повторных публикаций
- Проверка существования файлов изображений

## Файловая структура
```
NEWSMAKER/
├── data/
│   ├── daily_news_YYYY-MM-DD.json    # Новости за день
│   └── images/
│       └── YYYY-MM-DD/
│           ├── news_20250720_1.png   # Приоритет 1
│           ├── news_20250720_2.png   # Приоритет 2
│           └── ...                   # До приоритета 7
├── logs/
│   ├── newsmaker.log                 # Основные логи
│   ├── errors.log                    # Только ошибки
│   └── message_history.json          # Антидубликатор
└── [модули Python]
```

## Логирование
- Основной лог: `logs/newsmaker.log`
- Только ошибки: `logs/errors.log`
- История сообщений: `logs/message_history.json`
- Автоматическая ротация файлов

## Зависимости
- requests 2.31.0 - HTTP запросы
- python-telegram-bot 20.7 - Telegram API
- schedule 1.2.0 - планировщик задач
- python-dotenv 1.0.0 - работа с .env
- loguru 0.7.2 - логирование
- openai 1.12.0 - OpenAI API для изображений

## Миграция со старой архитектуры

### Что изменилось:
- ❌ **Убрано:** Генерация изображений во время публикации
- ❌ **Убрано:** Расписание каждые 3 часа
- ✅ **Добавлено:** Предварительная генерация всех изображений
- ✅ **Добавлено:** 7 новостей вместо 1-6
- ✅ **Добавлено:** Равномерное распределение на весь день

### Совместимость:
- Все старые CLI команды работают
- `scheduler.py` автоматически переключается в новый режим
- Legacy тесты сохранены для отладки

## Основные точки для модификации

### Промпты (prompts.py)
- **Промпт для 7 новостей**: `get_perplexity_daily_collection_prompt()`
- **4-панельный комикс**: `get_openai_comic_prompt()`
- **Настройки изображений**: `PromptConfig.OPENAI_IMAGE_QUALITY = "medium"`

### Расписание (config.py)
```python
COLLECTION_TIME = "08:30"  # Время сбора
PUBLICATION_SCHEDULE = [   # 7 времён публикации
    "09:05", "11:03", "13:07", "15:09", 
    "17:05", "19:02", "21:07"
]
```

### Тестирование
- `python main.py --test` - проверка всех компонентов
- `python main.py --test-publish-all` - публикация всех новостей подряд
- `python main.py --status` - текущий статус системы

## Мониторинг и отладка

### Проверка статуса:
```bash
python main.py --status
```
Показывает:
- Файл новостей за вчера (дата, количество)
- Статус публикаций (опубликовано/ожидает)
- Детали по каждой новости

### Логи для отладки:
- `tail -f logs/newsmaker.log` - основные события
- `tail -f logs/errors.log` - только ошибки
- Поиск по времени и приоритету новостей

### Ручное управление:
- Принудительная публикация: `--force-publish N`
- Повторный сбор: удалить файл + `--collect`
- Тестовая публикация всех: `--test-publish-all`

## Важные напоминания для поддержки

1. **Deep Research** может возвращать теги `<think>` - автоматически очищаются
2. **Приоритеты 1-7** теперь, не 1-5
3. **Изображения генерируются только при сборе**, не при публикации
4. **Quality "medium"** экономит деньги, но всё ещё хорошее качество
5. **Все файлы относительные пути** для переносимости

Система оптимизирована для стабильной ежедневной работы с минимальным вмешательством.