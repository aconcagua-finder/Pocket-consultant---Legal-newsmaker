# 🎛️ Веб-интерфейс управления NEWSMAKER

## 📋 Описание

Удобный веб-интерфейс для настройки всех параметров системы NEWSMAKER без необходимости редактирования файлов конфигурации вручную.

## 🚀 Запуск

### 1. Установка зависимостей

```bash
pip install Flask Flask-CORS
# или
pip install -r requirements.txt
```

### 2. Запуск веб-сервера

```bash
python web_config_app.py
```

### 3. Открытие интерфейса

Откройте в браузере: **http://localhost:5000**

## 🎯 Возможности

### 📊 Вкладки настроек

#### 🤖 **AI Модели**
- **Perplexity AI**
  - Выбор модели (sonar, sonar-pro, sonar-deep-research и др.)
  - Настройка max_tokens (1000-16384)
  - Temperature (0.0-1.0) - креативность ответов
  - Top P (0.0-1.0) - разнообразие токенов
  
- **OpenAI**
  - Выбор модели (DALL-E 2, DALL-E 3, GPT-Image-1)
  - Качество изображений (standard/hd)
  - Стиль (vivid/natural)
  - Размер (1024x1024, 1024x1792, 1792x1024)

#### 📅 **Расписание**
- Время сбора новостей (по умолчанию 08:30 МСК)
- 7 времён публикации для 7 новостей
- Все времена в московском часовом поясе

#### 📝 **Промпты**
- Системный промпт для Perplexity
- Промпт для сбора новостей
- Промпт для генерации комиксов
- Полная кастомизация текста запросов

#### 📰 **Контент**
- Количество новостей в день (1-10)
- Минимальная длина контента
- Максимальная длина контента
- Приоритеты новостей

#### 💾 **Хранение**
- Срок хранения файлов новостей
- Размер истории сообщений
- Срок хранения истории

#### 📊 **Мониторинг**
- Уровень логирования (DEBUG/INFO/WARNING/ERROR)
- Режим отладки
- Тестовый режим (без реальной публикации)

#### 🕐 **История**
- Просмотр истории изменений
- Восстановление из истории
- Экспорт/импорт конфигурации

## 🔧 Функции

### ✨ Основные функции

1. **💾 Сохранение** - сохраняет все изменения в файлы конфигурации
2. **↺ Сброс по секциям** - сброс отдельной секции к дефолтным значениям
3. **🔄 Полный сброс** - возврат всех настроек к изначальным
4. **📥 Экспорт** - скачивание конфигурации в JSON
5. **📤 Импорт** - загрузка конфигурации из файла

### 🛡️ Валидация

Система автоматически проверяет:
- Корректность выбранных моделей
- Правильность формата времени
- Соответствие лимитов контента
- Логическую согласованность параметров

### 📊 Индикаторы

- **🟢 Зеленая точка** - система активна и работает
- **🔴 Красная точка** - проблемы с подключением
- **Последнее сохранение** - время последнего успешного сохранения

## 🎨 Интерфейс

### Особенности дизайна

- **Современный градиентный дизайн**
- **Адаптивная верстка** для всех устройств
- **Анимированные переходы** между вкладками
- **Интуитивные контролы** (ползунки, селекторы)
- **Подсказки** для сложных параметров
- **Визуальная обратная связь** при изменениях

### Цветовая схема

- **Основной** - градиент фиолетовый (#667eea → #764ba2)
- **Успех** - зеленый (#28a745)
- **Предупреждение** - желтый (#ffc107)
- **Ошибка** - красный (#dc3545)
- **Информация** - серый (#6c757d)

## 📁 Файлы конфигурации

### Создаваемые файлы

- **config_web.json** - основная конфигурация из веб-интерфейса
- **config_defaults.json** - дефолтные настройки для сброса
- **prompts_custom.json** - кастомные промпты
- **config_updates.py** - Python код для применения изменений

### Интеграция с основной системой

После сохранения настроек через веб-интерфейс:

1. Изменения сохраняются в `config_web.json`
2. Создается файл `config_updates.py` с Python кодом
3. Для применения изменений в основной системе импортируйте обновления:

```python
# В файле config.py добавьте:
try:
    from config_updates import *
except ImportError:
    pass  # Используются дефолтные настройки
```

## 🔐 Безопасность

- Случайный секретный ключ для сессий
- Валидация всех входных данных
- Защита от XSS и инъекций
- Локальный доступ по умолчанию (localhost)

Для доступа с других устройств измените в `web_config_app.py`:
```python
app.run(debug=False, host='0.0.0.0', port=5000)
```

## 🐛 Устранение неполадок

### Проблема: Не открывается интерфейс

**Решение:**
1. Проверьте, что Flask установлен: `pip install Flask Flask-CORS`
2. Убедитесь, что порт 5000 не занят
3. Проверьте файрвол/антивирус

### Проблема: Изменения не сохраняются

**Решение:**
1. Проверьте права на запись в директории проекта
2. Убедитесь, что нажали кнопку "Сохранить"
3. Проверьте логи в консоли

### Проблема: Настройки не применяются в основной системе

**Решение:**
1. Импортируйте `config_updates.py` в основной `config.py`
2. Перезапустите основную систему после изменений
3. Проверьте, что файлы конфигурации читаются корректно

## 📚 API Endpoints

Веб-интерфейс предоставляет REST API:

- **GET /api/config** - получить текущую конфигурацию
- **POST /api/config** - обновить конфигурацию
- **POST /api/reset** - сбросить настройки
- **GET /api/history** - получить историю изменений
- **POST /api/restore** - восстановить из истории
- **GET /api/export** - экспортировать конфигурацию
- **POST /api/import** - импортировать конфигурацию

## 🎯 Советы по использованию

1. **Делайте резервные копии** - используйте экспорт перед важными изменениями
2. **Тестируйте изменения** - используйте тестовый режим для проверки
3. **Следите за валидацией** - обращайте внимание на предупреждения
4. **Используйте историю** - можно откатиться к предыдущим настройкам
5. **Документируйте изменения** - экспортируйте конфигурации с датами

## 🚀 Быстрый старт

```bash
# 1. Установка зависимостей
pip install Flask Flask-CORS

# 2. Запуск интерфейса
python web_config_app.py

# 3. Открытие в браузере
# Перейдите на http://localhost:5000

# 4. Настройка параметров
# Измените нужные настройки

# 5. Сохранение
# Нажмите кнопку "Сохранить"

# 6. Применение в системе
# Перезапустите основную систему
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Посмотрите файл `logs/newsmaker.log`
3. Убедитесь в корректности API ключей
4. Проверьте валидацию в интерфейсе

---

💡 **Совет:** Держите веб-интерфейс открытым во время работы системы для мониторинга статуса и быстрого изменения настроек!